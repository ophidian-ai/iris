import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  weight: ["400", "700", "900"],
});

export const metadata: Metadata = {
  title: "Point of Hope Apostolic Church | Experience Pentecost",
  description:
    "Welcome to Point of Hope Apostolic Church in Indianapolis, IN. Join us for worship, community, and spiritual growth. Pastor Stephen Gossage.",
  openGraph: {
    title: "Point of Hope Apostolic Church",
    description:
      "A welcoming community of faith in Indianapolis. Join us for Sunday worship and experience the hope of Pentecost.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${playfair.variable} antialiased font-sans`}
      >
        {children}
      </body>
    </html>
  );
}
